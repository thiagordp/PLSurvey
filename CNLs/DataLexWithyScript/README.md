Load the yscript files into DataLex.

Available goals:

- portabilityRight
- directtransmissionRight

When the consultation starts, DataLex will ask which goal to evaluate.

The system will then ask the user any questions required to determine the truth of the facts used in the rules.

The FORWARD rules are evaluated automatically whenever their triggering facts become known and may derive obligations during the consultation.